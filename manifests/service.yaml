apiVersion: v1
kind: Service
metadata:
  name: wf1differentBranch
  namespace: namespace-workflow-1725581919175
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: wf1differentBranch
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000