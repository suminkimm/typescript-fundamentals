apiVersion: v1
kind: Service
metadata:
  name: wf1SameBranch
  namespace: namespace-workflow-1725581577715
  labels:
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app: wf1SameBranch
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000