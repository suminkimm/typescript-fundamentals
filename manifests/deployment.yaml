apiVersion: apps/v1
kind: Deployment
metadata:
  name: diffrepo
  labels:
    app: diffrepo
    kubernetes.azure.com/generator: devhub
  namespace: namespace-workflow-1726175363172
spec:
  replicas: 1
  selector:
    matchLabels:
      app: diffrepo
  template:
    metadata:
      labels:
        app: diffrepo
    spec:
      containers:
        - name: diffrepo
          image: acrworkflow1726175363172.azurecr.io/image-workflow-1726175363172:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
